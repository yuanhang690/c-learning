分析（上图）：主函数中，将二维数组arr传递到printarr2函数“arr”为首元素地址（二维数组的数组名表示首元素地址，二维数组首元素是第一行,
第一行看成五个整形元素的一维数组），printarr2
中以“int (*p)[5]”（指向一维数组的指针）接收；
printarr2函数中对于printf，
①（p+i）表示某一行的地址
②*（p+i）找到这一行的数组名（该行的首元素)
③*（*（p+i）+j）找到第i行下标为j的元素